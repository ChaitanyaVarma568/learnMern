<section>

    <h1>Good Day!</h1>
     <% if (day === "saturday" || day === "sunday") { %>
        <h2>It's the weekend! Enjoy your day off!</h2>
     <% } else { %>
        <h2>It's the weekday! Enjoy your work!</h2>
     <% } %>
</section>